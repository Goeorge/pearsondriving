import { Inter, Lexend, Raleway, Roboto,  } from "next/font/google";

export const inter = Inter({ 
  subsets: ["latin"],
  display:'swap' });

export const lexend = Lexend({ 
  subsets: ["latin"],
  display:'swap' });

export const raleway = Raleway({ 
subsets: ["latin"],
display:'swap' });

// export const roboto = Roboto({ });